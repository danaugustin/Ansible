# templates/secret_template.yml.j2
apiVersion: v1
kind: Secret
metadata:
  name: "{{ secret_name }}"
  namespace: "{{ namespace }}"
type: {{ secret_type }}
data:
  username: "{{ username | b64encode }}"
  password: "{{ password | b64encode }}"